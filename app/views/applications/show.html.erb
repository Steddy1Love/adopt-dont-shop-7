<h1>Application #<%= @application.id %></h1>
<h3> Application Status: <%= @application.status %> </h3>

<br>

<h3>Applicant Name: <%= @application.name %></h3>

<p> Applicant's Address: 
    <%= @application.street_address %>,
    <%= @application.city %>,
    <%= @application.state %>
    <%= @application.zip_code %> </p>

<h3> Why will this home be a good home for the pet(s)? </h3>
<p> <%= @application.description %> </p>

<h3> Pets Being Applied For: </h3>
<ul>
    <% @app.each do |pet| %>
    <li> <%= link_to "#{pet.name}", "/pets/#{pet.id}" %> </li>
    <% end %>
</ul>

<h3>"Add a Pet to this Application"</h3>
<%= form_with url: "/applications/#{@application.id}?search", method: :get, data: { turbo: false } do |form| %>
    <%= form.label :pet_name, "Pet Name:" %>
    <%= text_field_tag :pet_name, params[:query] %><br>
    <%= form.submit 'Search by Pet Name' %>
<% end %>



